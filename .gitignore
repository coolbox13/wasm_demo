# Rust build artifacts
target/
Cargo.lock

# Spin runtime & logs
.spin/
spin
spin.sig
crt.pem

# Browser WASM build output (regenerated by wasm-pack)
browser/dist/

# Pre-built components (downloaded, not source)
spin_static_fs.wasm
